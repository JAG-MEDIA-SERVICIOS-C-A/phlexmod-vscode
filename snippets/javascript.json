{
  "MIA JS Module": {
    "prefix": "px-js",
    "body": [
      "/**",
      " * Nombre del Archivo: ${1:filename}.js",
      " * Descripción: Lógica del módulo ${2:ModuleName}",
      " * ",
      " * @package    PHLEXMOD-Framework",
      " * @subpackage ${3:Backend/modules/path}",
      " * @author     JAG-Media Servicios, C.A.",
      " * @version    v2.0.1",
      " * @since      ${CURRENT_YEAR}-${CURRENT_MONTH}-${CURRENT_DATE}",
      " */",
      "",
      "/*",
      " * \\${PATH_ENDPOINTS} ruta definida en engine.php",
      " * \\${PATH_UI} ruta definida en engine.php",
      " * \\${PATH_JS} ruta definida en engine.php",
      " */",
      "window.mod${2:ModuleName} = window.mod${2:ModuleName} || {};",
      "",
      "const mod${2:ModuleName} = {",
      "    config: {",
      "        endpoints: {",
      "            ${8:endpoint}: `\\${PATH_ENDPOINTS}${8:endpoint}.api.php`",
      "        },",
      "        ui: {",
      "            ${4:btnId}: '#${4:btnId}',",
      "            ${7:inputId}: '#${7:inputId}'",
      "        }",
      "    },",
      "",
      "    init: function() {",
      "        console.log('Módulo ${2:ModuleName} inicializado.');",
      "        this.setupAccessibility();",
      "        this.bindEvents();",
      "    },",
      "",
      "    /**",
      "     * Configuración de Accesibilidad (A11Y) - Normativa MIA",
      "     */",
      "    setupAccessibility: function() {",
      "        const self = this;",
      "        $(self.config.ui.${4:btnId}).attr('aria-label', '${5:Acción del botón}');",
      "    },",
      "",
      "    bindEvents: function() {",
      "        const self = this;",
      "        const ui = self.config.ui;",
      "        // Event listeners",
      "        $(ui.${4:btnId}).on('click', function(e) {",
      "             e.preventDefault();",
      "             self.handle${5:Action}();",
      "        });",
      "    },",
      "",
      "    handle${5:Action}: function() {",
      "        const self = this;",
      "        const ui = self.config.ui;",
      "        const data = {",
      "            ${6:param}: $(ui.${7:inputId}).val()",
      "        };",
      "",
      "        self.sendRequest(data);",
      "    },",
      "",
      "    sendRequest: function(payload) {",
      "        const self = this;",
      "        $.ajax({",
      "            url: self.config.endpoints.${8:endpoint},",
      "            method: 'POST',",
      "            data: payload,",
      "            dataType: 'json',",
      "            success: function(response) {",
      "                if (response.status === 'success') {",
      "                    alert('Operación exitosa');",
      "                } else {",
      "                    alert('Error: ' + response.message);",
      "                }",
      "            },",
      "            error: function(xhr, status, error) {",
      "                console.error('Error:', error);",
      "            }",
      "        });",
      "    }",
      "};",
      "",
      "$(document).ready(function() {",
      "    mod${2:ModuleName}.init();",
      "});"
    ],
    "description": "Template para lógica JS modular (MIA Namespace Pattern)"
  }
}